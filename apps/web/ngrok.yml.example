version: "2"
authtoken: YOUR_NGROK_AUTH_TOKEN_HERE
tunnels:
  minipay:
    proto: http
    addr: 3000
    inspect: true
    bind_tls: true
    # Optional: set a custom domain if you have one
    # hostname: your-domain.com
    # Or use a static subdomain (requires paid plan):
    # subdomain: color-match-rush

